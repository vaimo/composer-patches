patch:validate

patch:apply

> test "$(cat composer.lock|grep '\"patches_applied\"')" == ""

patch:redo

> test "$(cat composer.lock|grep '\"patches_applied\"')" == ""

patch:apply
patch:apply

> test "$(cat composer.lock|grep '\"patches_applied\"')" == ""

patch:undo

> test "$(cat composer.lock|grep '\"patches_applied\"')" == ""

patch:apply

> test "$(cat composer.lock|grep '\"patches_applied\"')" == ""

patch:redo
patch:redo

> test "$(cat composer.lock|grep '\"patches_applied\"')" == ""

patch:undo

> test "$(cat composer.lock|grep '\"patches_applied\"')" == ""