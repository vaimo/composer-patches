version: "3.8"
services:
  devbox:
    container_name: composer-patches
    build:
      dockerfile: Dockerfile
      context: .
    volumes:
      - ./src:/module/src
    entrypoint: tail -f /dev/null
